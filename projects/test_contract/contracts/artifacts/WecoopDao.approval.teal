#pragma version 10

// This TEAL was generated by TEALScript v0.102.0
// https://github.com/algorandfoundation/TEALScript

// This contract is compliant with and/or implements the following ARCs: [ ARC4 ]

// The following ten lines of TEAL handle initial program flow
// This pattern is used to make it easy for anyone to parse the start of the program and determine if a specific action is allowed
// Here, action refers to the OnComplete in combination with whether the app is being created or called
// Every possible action for this contract is represented in the switch statement
// If the action is not implemented in the contract, its respective branch will be "*NOT_IMPLEMENTED" which just contains "err"
txn ApplicationID
!
int 6
*
txn OnCompletion
+
switch *call_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *create_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED

*NOT_IMPLEMENTED:
	// The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?
	err

// createApplication()void
*abi_route_createApplication:
	// execute createApplication()void
	callsub createApplication
	int 1
	return

// createApplication(): void
createApplication:
	proto 0 0

	// contracts/WecoopDao.algo.ts:39
	// this.totalPolls.value = 0
	byte 0x746f74616c506f6c6c73 // "totalPolls"
	int 0
	app_global_put

	// contracts/WecoopDao.algo.ts:40
	// this.totalVotes.value = 0
	byte 0x746f74616c566f746573 // "totalVotes"
	int 0
	app_global_put
	retsub

// optinToAsset(pay,uint64)void
*abi_route_optinToAsset:
	// asset: uint64
	txna ApplicationArgs 1
	btoi

	// mbrTxn: pay
	txn GroupIndex
	int 1
	-
	dup
	gtxns TypeEnum
	int pay
	==

	// argument 1 (mbrTxn) for optinToAsset must be a pay transaction
	assert

	// execute optinToAsset(pay,uint64)void
	callsub optinToAsset
	int 1
	return

// optinToAsset(mbrTxn: PayTxn, asset: AssetID): void
optinToAsset:
	proto 2 0

	// contracts/WecoopDao.algo.ts:46
	// assert(this.txn.sender === this.app.creator, 'Error: Not the creator trying to optin to an asset')
	txn Sender
	txna Applications 0
	app_params_get AppCreator
	pop
	==

	// Error: Not the creator trying to optin to an asset
	assert

	// contracts/WecoopDao.algo.ts:49
	// assert(mbrTxn.receiver === this.app.address, 'Receiver is not the app address')
	frame_dig -1 // mbrTxn: PayTxn
	gtxns Receiver
	global CurrentApplicationAddress
	==

	// Receiver is not the app address
	assert

	// contracts/WecoopDao.algo.ts:52
	// assert(!this.app.address.isOptedInToAsset(asset))
	global CurrentApplicationAddress
	frame_dig -2 // asset: AssetID
	asset_holding_get AssetBalance
	swap
	pop
	!
	assert

	// contracts/WecoopDao.algo.ts:55
	// verifyPayTxn(mbrTxn, { amount: { greaterThanEqualTo: globals.minBalance + globals.assetOptInMinBalance } })
	// verify amount
	frame_dig -1 // mbrTxn: PayTxn
	gtxns Amount
	global MinBalance
	global AssetOptInMinBalance
	+
	>=

	// transaction verification failed: {"txn":"mbrTxn","field":"amount","condition":"greaterThanEqualTo","expected":">=globals.minBalance + globals.assetOptInMinBalance"}
	assert

	// contracts/WecoopDao.algo.ts:58
	// sendAssetTransfer({ assetReceiver: this.app.address, assetAmount: 0, xferAsset: asset })
	itxn_begin
	int axfer
	itxn_field TypeEnum

	// contracts/WecoopDao.algo.ts:58
	// assetReceiver: this.app.address
	global CurrentApplicationAddress
	itxn_field AssetReceiver

	// contracts/WecoopDao.algo.ts:58
	// assetAmount: 0
	int 0
	itxn_field AssetAmount

	// contracts/WecoopDao.algo.ts:58
	// xferAsset: asset
	frame_dig -2 // asset: AssetID
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	int 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	retsub

// createPoll(pay,axfer,string)(address,uint64,string,uint64,uint64,uint64)
*abi_route_createPoll:
	// The ABI return prefix
	byte 0x151f7c75

	// question: string
	txna ApplicationArgs 1
	extract 2 0

	// axfer: axfer
	txn GroupIndex
	int 1
	-
	dup
	gtxns TypeEnum
	int axfer
	==

	// argument 1 (axfer) for createPoll must be a axfer transaction
	assert

	// mbrTxn: pay
	txn GroupIndex
	int 2
	-
	dup
	gtxns TypeEnum
	int pay
	==

	// argument 2 (mbrTxn) for createPoll must be a pay transaction
	assert

	// execute createPoll(pay,axfer,string)(address,uint64,string,uint64,uint64,uint64)
	callsub createPoll
	concat
	log
	int 1
	return

// createPoll(mbrTxn: PayTxn, axfer: AssetTransferTxn, question: string): PollInfo
createPoll:
	proto 3 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	byte 0x
	dupn 4

	// contracts/WecoopDao.algo.ts:66
	// assert(this.app.address.isOptedInToAsset(axfer.xferAsset), 'Application not opted in to the asset')
	global CurrentApplicationAddress
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns XferAsset
	asset_holding_get AssetBalance
	swap
	pop

	// Application not opted in to the asset
	assert

	// contracts/WecoopDao.algo.ts:69
	// assert(axfer.assetReceiver === this.app.address, 'Deposit transaction not to the app wallet')
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns AssetReceiver
	global CurrentApplicationAddress
	==

	// Deposit transaction not to the app wallet
	assert

	// contracts/WecoopDao.algo.ts:72
	// creatorAddress: Address = axfer.sender
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns Sender
	frame_bury 0 // creatorAddress: Address

	// contracts/WecoopDao.algo.ts:75
	// currentNonce: uint64 = this.totalPolls.value
	byte 0x746f74616c506f6c6c73 // "totalPolls"
	app_global_get
	frame_bury 1 // currentNonce: uint64

	// contracts/WecoopDao.algo.ts:78
	// newNonce: uint64 = currentNonce === 0 ? currentNonce + 1 : 0
	frame_dig 1 // currentNonce: uint64
	int 0
	==
	bz *ternary0_false
	frame_dig 1 // currentNonce: uint64
	int 1
	+
	b *ternary0_end

*ternary0_false:
	int 0

*ternary0_end:
	frame_bury 2 // newNonce: uint64

	// contracts/WecoopDao.algo.ts:81
	// assert(!this.poll({ nonce: newNonce }).exists, 'This poll already exists!')
	byte 0x706f6c6c5f // "poll_"
	frame_dig 2 // newNonce: uint64
	itob
	concat
	box_len
	swap
	pop
	!

	// This poll already exists!
	assert

	// contracts/WecoopDao.algo.ts:84
	// verifyPayTxn(mbrTxn, { amount: pollMbr })
	// verify amount
	frame_dig -1 // mbrTxn: PayTxn
	gtxns Amount
	int 3450
	==

	// transaction verification failed: {"txn":"mbrTxn","field":"amount","expected":"pollMbr"}
	assert

	// contracts/WecoopDao.algo.ts:87
	// this.totalPolls.value += 1
	byte 0x746f74616c506f6c6c73 // "totalPolls"
	app_global_get
	int 1
	+
	byte 0x746f74616c506f6c6c73 // "totalPolls"
	swap
	app_global_put

	// contracts/WecoopDao.algo.ts:90
	// deposited = axfer.assetAmount
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns AssetAmount
	frame_bury 3 // deposited: uint64

	// contracts/WecoopDao.algo.ts:91
	// selecteAsset = axfer.xferAsset
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns XferAsset
	frame_bury 4 // selecteAsset: uint64

	// contracts/WecoopDao.algo.ts:94
	// this.poll({ nonce: newNonce }).value = {
	//       question: question,
	//       creator: creatorAddress,
	//       totalVotes: 0,
	//       yesVotes: 0,
	//       deposited: deposited,
	//       selected_asset: selecteAsset,
	//     }
	byte 0x706f6c6c5f // "poll_"
	frame_dig 2 // newNonce: uint64
	itob
	concat
	dup
	box_del
	pop
	byte 0x // initial head
	byte 0x // initial tail
	byte 0x0042 // initial head offset
	frame_dig 0 // creatorAddress: Address
	callsub *process_static_tuple_element
	frame_dig 4 // selecteAsset: uint64
	itob
	callsub *process_static_tuple_element
	frame_dig -3 // question: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	byte 0x0000000000000000
	callsub *process_static_tuple_element
	byte 0x0000000000000000
	callsub *process_static_tuple_element
	frame_dig 3 // deposited: uint64
	itob
	callsub *process_static_tuple_element
	pop // pop head offset
	concat // concat head and tail
	box_put

	// contracts/WecoopDao.algo.ts:104
	// return this.poll({ nonce: newNonce }).value;
	byte 0x706f6c6c5f // "poll_"
	frame_dig 2 // newNonce: uint64
	itob
	concat
	box_get

	// box value does not exist: this.poll({ nonce: newNonce }).value
	assert

	// set the subroutine return value
	frame_bury 0

	// pop all local variables from the stack
	popn 4
	retsub

// makeVote((uint64),axfer,pay,bool)void
*abi_route_makeVote:
	// inFavor: bool
	txna ApplicationArgs 2
	dup
	len
	int 1
	==

	// argument 0 (inFavor) for makeVote must be a bool
	assert
	int 0
	getbit

	// mbrTxn: pay
	txn GroupIndex
	int 1
	-
	dup
	gtxns TypeEnum
	int pay
	==

	// argument 1 (mbrTxn) for makeVote must be a pay transaction
	assert

	// axfer: axfer
	txn GroupIndex
	int 2
	-
	dup
	gtxns TypeEnum
	int axfer
	==

	// argument 2 (axfer) for makeVote must be a axfer transaction
	assert

	// pollId: (uint64)
	txna ApplicationArgs 1
	dup
	len
	int 8
	==

	// argument 3 (pollId) for makeVote must be a (uint64)
	assert

	// execute makeVote((uint64),axfer,pay,bool)void
	callsub makeVote
	int 1
	return

// makeVote(pollId: PollId, axfer: AssetTransferTxn, mbrTxn: PayTxn, inFavor: boolean): void
makeVote:
	proto 4 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	byte 0x
	dupn 3

	// contracts/WecoopDao.algo.ts:114
	// currentNonce: uint64 = this.poll(pollId).value.totalVotes
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 42 8
	btoi
	frame_bury 0 // currentNonce: uint64

	// contracts/WecoopDao.algo.ts:116
	// newNonce: uint64 = currentNonce === 0 ? currentNonce + 1 : 0
	frame_dig 0 // currentNonce: uint64
	int 0
	==
	bz *ternary1_false
	frame_dig 0 // currentNonce: uint64
	int 1
	+
	b *ternary1_end

*ternary1_false:
	int 0

*ternary1_end:
	frame_bury 1 // newNonce: uint64

	// contracts/WecoopDao.algo.ts:118
	// depositedAmount: uint64 = axfer.assetAmount
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns AssetAmount
	frame_bury 2 // depositedAmount: uint64

	// contracts/WecoopDao.algo.ts:120
	// depositedAsset: AssetID = axfer.xferAsset
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns XferAsset
	frame_bury 3 // depositedAsset: AssetID

	// contracts/WecoopDao.algo.ts:122
	// assert(this.poll(pollId).value.creator !== this.txn.sender, 'Poll creator cant vote')
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 0 32
	txn Sender
	!=

	// Poll creator cant vote
	assert

	// contracts/WecoopDao.algo.ts:124
	// assert(this.app.address.isOptedInToAsset(axfer.xferAsset), 'Application not opted in to the asset')
	global CurrentApplicationAddress
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns XferAsset
	asset_holding_get AssetBalance
	swap
	pop

	// Application not opted in to the asset
	assert

	// contracts/WecoopDao.algo.ts:127
	// assert(axfer.assetReceiver === this.app.address, 'Deposit transaction not to the app wallet')
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns AssetReceiver
	global CurrentApplicationAddress
	==

	// Deposit transaction not to the app wallet
	assert

	// contracts/WecoopDao.algo.ts:130
	// assert(this.poll(pollId).exists, 'Poll user is trying to vote in does not exist')
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_len
	swap
	pop

	// Poll user is trying to vote in does not exist
	assert

	// contracts/WecoopDao.algo.ts:133
	// assert(depositedAsset === this.poll(pollId).value.selected_asset, 'using wrong asset to vote into this poll')
	frame_dig 3 // depositedAsset: AssetID
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 32 8
	btoi
	==

	// using wrong asset to vote into this poll
	assert

	// contracts/WecoopDao.algo.ts:136
	// assert(!this.vote({ pollId: pollId, nonce: newNonce }).exists, 'Vote with new nonce already exists')
	byte 0x766f74655f // "vote_"
	frame_dig 1 // newNonce: uint64
	itob
	frame_dig -1 // pollId: PollId
	concat
	concat
	box_len
	swap
	pop
	!

	// Vote with new nonce already exists
	assert

	// contracts/WecoopDao.algo.ts:139
	// this.vote({ pollId: pollId, nonce: newNonce }).value = { voter: this.txn.sender, claimed: false }
	byte 0x766f74655f // "vote_"
	frame_dig 1 // newNonce: uint64
	itob
	frame_dig -1 // pollId: PollId
	concat
	concat
	byte 0x00
	int 0
	dup
	setbit
	txn Sender
	concat
	box_put

	// contracts/WecoopDao.algo.ts:142
	// this.poll(pollId).value.totalVotes += 1
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	store 255 // full array
	load 255 // full array
	int 42
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 42 8
	btoi
	int 1
	+
	itob
	replace3
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	dup
	box_del
	pop
	swap
	box_put

	// *if0_condition
	// contracts/WecoopDao.algo.ts:144
	// inFavor
	frame_dig -4 // inFavor: boolean
	bz *if0_end

	// *if0_consequent
	// contracts/WecoopDao.algo.ts:146
	// this.poll(pollId).value.yesVotes += 1
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	store 255 // full array
	load 255 // full array
	int 50
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 50 8
	btoi
	int 1
	+
	itob
	replace3
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	dup
	box_del
	pop
	swap
	box_put

*if0_end:
	retsub

// getPollByPollId((uint64))(address,uint64,string,uint64,uint64,uint64)
*abi_route_getPollByPollId:
	// The ABI return prefix
	byte 0x151f7c75

	// pollId: (uint64)
	txna ApplicationArgs 1
	dup
	len
	int 8
	==

	// argument 0 (pollId) for getPollByPollId must be a (uint64)
	assert

	// execute getPollByPollId((uint64))(address,uint64,string,uint64,uint64,uint64)
	callsub getPollByPollId
	concat
	log
	int 1
	return

// getPollByPollId(pollId: PollId): PollInfo
getPollByPollId:
	proto 1 1

	// contracts/WecoopDao.algo.ts:151
	// assert(this.poll(pollId).exists, 'Searched poll does not exist')
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_len
	swap
	pop

	// Searched poll does not exist
	assert

	// contracts/WecoopDao.algo.ts:152
	// return this.poll(pollId).value;
	byte 0x706f6c6c5f // "poll_"
	frame_dig -1 // pollId: PollId
	concat
	box_get

	// box value does not exist: this.poll(pollId).value
	assert
	retsub

// getVoteByVoteId((uint64,(uint64)))(bool,address)
*abi_route_getVoteByVoteId:
	// The ABI return prefix
	byte 0x151f7c75

	// voteId: (uint64,(uint64))
	txna ApplicationArgs 1
	dup
	len
	int 16
	==

	// argument 0 (voteId) for getVoteByVoteId must be a (uint64,(uint64))
	assert

	// execute getVoteByVoteId((uint64,(uint64)))(bool,address)
	callsub getVoteByVoteId
	concat
	log
	int 1
	return

// getVoteByVoteId(voteId: VoteId): VoteInfo
getVoteByVoteId:
	proto 1 1

	// contracts/WecoopDao.algo.ts:156
	// assert(this.vote(voteId).exists, 'searched vote does not exist')
	byte 0x766f74655f // "vote_"
	frame_dig -1 // voteId: VoteId
	concat
	box_len
	swap
	pop

	// searched vote does not exist
	assert

	// contracts/WecoopDao.algo.ts:157
	// return this.vote(voteId).value;
	byte 0x766f74655f // "vote_"
	frame_dig -1 // voteId: VoteId
	concat
	box_get

	// box value does not exist: this.vote(voteId).value
	assert
	retsub

*create_NoOp:
	method "createApplication()void"
	txna ApplicationArgs 0
	match *abi_route_createApplication

	// this contract does not implement the given ABI method for create NoOp
	err

*call_NoOp:
	method "optinToAsset(pay,uint64)void"
	method "createPoll(pay,axfer,string)(address,uint64,string,uint64,uint64,uint64)"
	method "makeVote((uint64),axfer,pay,bool)void"
	method "getPollByPollId((uint64))(address,uint64,string,uint64,uint64,uint64)"
	method "getVoteByVoteId((uint64,(uint64)))(bool,address)"
	txna ApplicationArgs 0
	match *abi_route_optinToAsset *abi_route_createPoll *abi_route_makeVote *abi_route_getPollByPollId *abi_route_getVoteByVoteId

	// this contract does not implement the given ABI method for call NoOp
	err

*process_static_tuple_element:
	proto 4 3
	frame_dig -4 // tuple head
	frame_dig -1 // element
	concat
	frame_dig -3 // tuple tail
	frame_dig -2 // head offset
	retsub

*process_dynamic_tuple_element:
	proto 4 3
	frame_dig -4 // tuple head
	frame_dig -2 // head offset
	concat
	frame_bury -4 // tuple head
	frame_dig -1 // element
	dup
	len
	frame_dig -2 // head offset
	btoi
	+
	itob
	extract 6 2
	frame_bury -2 // head offset
	frame_dig -3 // tuple tail
	swap
	concat
	frame_bury -3 // tuple tail
	frame_dig -4 // tuple head
	frame_dig -3 // tuple tail
	frame_dig -2 // head offset
	retsub