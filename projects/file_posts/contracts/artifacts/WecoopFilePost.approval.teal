#pragma version 10
intcblock 1 4 0 2 8 3
bytecblock 0x66696c65506f73745f 0x 0x6d616e616765725f61646472657373 0x746f74616c46696c65506f737473 0x746f74616c4c696b6573 0x0000000000000000 0x66696c65506f73744c696b655f

// This TEAL was generated by TEALScript v0.105.3
// https://github.com/algorandfoundation/TEALScript

// This contract is compliant with and/or implements the following ARCs: [ ARC4 ]

// The following ten lines of TEAL handle initial program flow
// This pattern is used to make it easy for anyone to parse the start of the program and determine if a specific action is allowed
// Here, action refers to the OnComplete in combination with whether the app is being created or called
// Every possible action for this contract is represented in the switch statement
// If the action is not implemented in the contract, its respective branch will be "*NOT_IMPLEMENTED" which just contains "err"
txn ApplicationID
!
pushint 6
*
txn OnCompletion
+
switch *call_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *create_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED

*NOT_IMPLEMENTED:
	// The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?
	err

// createApplication()void
*abi_route_createApplication:
	// execute createApplication()void
	callsub createApplication
	intc 0 // 1
	return

// createApplication(): void
createApplication:
	proto 0 0

	// contracts/WecoopFilePost.algo.ts:64
	// this.totalFilePosts.value = 0
	bytec 3 //  "totalFilePosts"
	intc 2 // 0
	app_global_put

	// contracts/WecoopFilePost.algo.ts:65
	// this.totalLikes.value = 0
	bytec 4 //  "totalLikes"
	intc 2 // 0
	app_global_put

	// contracts/WecoopFilePost.algo.ts:66
	// this.manager_address.value = this.txn.sender
	bytec 2 //  "manager_address"
	txn Sender
	app_global_put
	retsub

// optinToAsset(uint64,pay)void
*abi_route_optinToAsset:
	// mbrTxn: pay
	txn GroupIndex
	intc 0 // 1
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 0 (mbrTxn) for optinToAsset must be a pay transaction
	assert

	// assetId: uint64
	txna ApplicationArgs 1
	btoi

	// execute optinToAsset(uint64,pay)void
	callsub optinToAsset
	intc 0 // 1
	return

// optinToAsset(assetId: AssetID, mbrTxn: PayTxn): void
optinToAsset:
	proto 2 0

	// contracts/WecoopFilePost.algo.ts:70
	// assert(this.txn.sender == this.manager_address.value, 'Only the manager can opt this contract to a new asset')
	txn Sender
	bytec 2 //  "manager_address"
	app_global_get
	==

	// Only the manager can opt this contract to a new asset
	assert

	// contracts/WecoopFilePost.algo.ts:71
	// sendAssetTransfer({ assetReceiver: this.app.address, assetAmount: 0, xferAsset: assetId })
	itxn_begin
	intc 1 //  axfer
	itxn_field TypeEnum

	// contracts/WecoopFilePost.algo.ts:71
	// assetReceiver: this.app.address
	global CurrentApplicationAddress
	itxn_field AssetReceiver

	// contracts/WecoopFilePost.algo.ts:71
	// assetAmount: 0
	intc 2 // 0
	itxn_field AssetAmount

	// contracts/WecoopFilePost.algo.ts:71
	// xferAsset: assetId
	frame_dig -1 // assetId: AssetID
	itxn_field XferAsset

	// Fee field not set, defaulting to 0
	intc 2 // 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	retsub

// createFilePost(pay,axfer,string,string,string,string)void
*abi_route_createFilePost:
	// text: string
	txna ApplicationArgs 4
	extract 2 0

	// fileFormat: string
	txna ApplicationArgs 3
	extract 2 0

	// cid: string
	txna ApplicationArgs 2
	extract 2 0

	// country: string
	txna ApplicationArgs 1
	extract 2 0

	// axfer: axfer
	txn GroupIndex
	intc 0 // 1
	-
	dup
	gtxns TypeEnum
	intc 1 //  axfer
	==

	// argument 4 (axfer) for createFilePost must be a axfer transaction
	assert

	// mbrTxn: pay
	txn GroupIndex
	intc 3 // 2
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 5 (mbrTxn) for createFilePost must be a pay transaction
	assert

	// execute createFilePost(pay,axfer,string,string,string,string)void
	callsub createFilePost
	intc 0 // 1
	return

// createFilePost(mbrTxn: PayTxn, axfer: AssetTransferTxn, country: string, cid: string, fileFormat: string, text: string): void
createFilePost:
	proto 6 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 1 // 0x
	dupn 2

	// contracts/WecoopFilePost.algo.ts:84
	// assert(this.app.address.isOptedInToAsset(axfer.xferAsset), 'Application not opted in to the asset')
	global CurrentApplicationAddress
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns XferAsset
	asset_holding_get AssetBalance
	swap
	pop

	// Application not opted in to the asset
	assert

	// contracts/WecoopFilePost.algo.ts:87
	// assert(axfer.assetReceiver === this.app.address, 'Deposit transaction not to the app wallet')
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns AssetReceiver
	global CurrentApplicationAddress
	==

	// Deposit transaction not to the app wallet
	assert

	// contracts/WecoopFilePost.algo.ts:90
	// creatorAddress: Address = axfer.sender
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns Sender
	frame_bury 0 // creatorAddress: Address

	// contracts/WecoopFilePost.algo.ts:91
	// currentNonce: uint64 = this.totalFilePosts.value
	bytec 3 //  "totalFilePosts"
	app_global_get
	frame_bury 1 // currentNonce: uint64

	// contracts/WecoopFilePost.algo.ts:92
	// newNonce: uint64 = currentNonce + 1
	frame_dig 1 // currentNonce: uint64
	intc 0 // 1
	+
	frame_bury 2 // newNonce: uint64

	// contracts/WecoopFilePost.algo.ts:95
	// assert(!this.filePosts({ nonce: newNonce }).exists, 'This file post already exists!')
	bytec 0 //  "filePost_"
	frame_dig 2 // newNonce: uint64
	itob
	concat
	box_len
	swap
	pop
	!

	// This file post already exists!
	assert

	// contracts/WecoopFilePost.algo.ts:98
	// verifyPayTxn(mbrTxn, { amount: { greaterThanEqualTo: filePostMbr } })
	// verify amount
	frame_dig -1 // mbrTxn: PayTxn
	gtxns Amount
	pushint 4470
	>=

	// transaction verification failed: {"txn":"mbrTxn","field":"amount","condition":"greaterThanEqualTo","expected":">=filePostMbr"}
	assert

	// contracts/WecoopFilePost.algo.ts:101
	// this.totalFilePosts.value += 1
	bytec 3 //  "totalFilePosts"
	app_global_get
	intc 0 // 1
	+
	bytec 3 //  "totalFilePosts"
	swap
	app_global_put

	// contracts/WecoopFilePost.algo.ts:104
	// this.filePosts({ nonce: newNonce }).value = {
	//       text: text,
	//       creator_address: creatorAddress,
	//       likes: 0,
	//       replies: 0,
	//       strikes: 0,
	//       asset_id: axfer.xferAsset,
	//       timestamp: globals.latestTimestamp,
	//       country: country,
	//       file_cid: cid,
	//       file_format: fileFormat,
	//     }
	bytec 0 //  "filePost_"
	frame_dig 2 // newNonce: uint64
	itob
	concat
	dup
	box_del
	pop
	bytec 1 //  initial head
	bytec 1 //  initial tail
	pushbytes 0x0050 // initial head offset
	frame_dig 0 // creatorAddress: Address
	callsub *process_static_tuple_element
	global LatestTimestamp
	itob
	callsub *process_static_tuple_element
	bytec 5 // 0x0000000000000000
	callsub *process_static_tuple_element
	bytec 5 // 0x0000000000000000
	callsub *process_static_tuple_element
	bytec 5 // 0x0000000000000000
	callsub *process_static_tuple_element
	frame_dig -2 // axfer: AssetTransferTxn
	gtxns XferAsset
	itob
	callsub *process_static_tuple_element
	frame_dig -3 // country: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	frame_dig -4 // cid: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	frame_dig -5 // fileFormat: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	frame_dig -6 // text: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	pop // pop head offset
	concat // concat head and tail
	box_put
	retsub

// likeFilePost((uint64),pay,pay,axfer,axfer)void
*abi_route_likeFilePost:
	// platformCommunityFeeTxn: axfer
	txn GroupIndex
	intc 0 // 1
	-
	dup
	gtxns TypeEnum
	intc 1 //  axfer
	==

	// argument 0 (platformCommunityFeeTxn) for likeFilePost must be a axfer transaction
	assert

	// creatorPayTxn: axfer
	txn GroupIndex
	intc 3 // 2
	-
	dup
	gtxns TypeEnum
	intc 1 //  axfer
	==

	// argument 1 (creatorPayTxn) for likeFilePost must be a axfer transaction
	assert

	// platformAlgoFeeTxn: pay
	txn GroupIndex
	intc 5 // 3
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 2 (platformAlgoFeeTxn) for likeFilePost must be a pay transaction
	assert

	// mbrTxn: pay
	txn GroupIndex
	intc 1 // 4
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 3 (mbrTxn) for likeFilePost must be a pay transaction
	assert

	// filePostId: (uint64)
	txna ApplicationArgs 1
	dup
	len
	intc 4 // 8
	==

	// argument 4 (filePostId) for likeFilePost must be a (uint64)
	assert

	// execute likeFilePost((uint64),pay,pay,axfer,axfer)void
	callsub likeFilePost
	intc 0 // 1
	return

// likeFilePost(filePostId: FilePostId, mbrTxn: PayTxn, platformAlgoFeeTxn: PayTxn, creatorPayTxn: AssetTransferTxn, platformCommunityFeeTxn: AssetTransferTxn): void
likeFilePost:
	proto 5 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 1 // 0x

	// contracts/WecoopFilePost.algo.ts:125
	// assert(this.filePosts(filePostId).exists, 'File post that is trying to be liked does not exist')
	bytec 0 //  "filePost_"
	frame_dig -1 // filePostId: FilePostId
	concat
	box_len
	swap
	pop

	// File post that is trying to be liked does not exist
	assert

	// contracts/WecoopFilePost.algo.ts:127
	// assert(!this.filePostLikes({ postId: filePostId }).exists, 'User already liked this File post')
	bytec 6 //  "filePostLike_"
	frame_dig -1 // filePostId: FilePostId
	concat
	box_len
	swap
	pop
	!

	// User already liked this File post
	assert

	// contracts/WecoopFilePost.algo.ts:128
	// likedFilePost: FilePostData = this.filePosts(filePostId).value
	bytec 0 //  "filePost_"
	frame_dig -1 // filePostId: FilePostId
	concat
	frame_bury 0 // storage key//likedFilePost

	// contracts/WecoopFilePost.algo.ts:131
	// verifyPayTxn(mbrTxn, { amount: { greaterThanEqualTo: mbrTxn.amount } })
	// verify amount
	frame_dig -2 // mbrTxn: PayTxn
	gtxns Amount
	frame_dig -2 // mbrTxn: PayTxn
	gtxns Amount
	>=

	// transaction verification failed: {"txn":"mbrTxn","field":"amount","condition":"greaterThanEqualTo","expected":">=mbrTxn.amount"}
	assert

	// contracts/WecoopFilePost.algo.ts:134
	// verifyPayTxn(platformAlgoFeeTxn, { receiver: this.manager_address.value })
	// verify receiver
	frame_dig -3 // platformAlgoFeeTxn: PayTxn
	gtxns Receiver
	bytec 2 //  "manager_address"
	app_global_get
	==

	// transaction verification failed: {"txn":"platformAlgoFeeTxn","field":"receiver","expected":"this.manager_address.value"}
	assert

	// contracts/WecoopFilePost.algo.ts:137
	// assert(
	//       creatorPayTxn.assetReceiver == likedFilePost.creator_address,
	//       'Creator fee transaction for like is not to the creator address'
	//     )
	frame_dig -4 // creatorPayTxn: AssetTransferTxn
	gtxns AssetReceiver
	frame_dig 0 // storage key//likedFilePost
	box_get

	// box value does not exist: this.filePosts(filePostId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 0 32
	==

	// Creator fee transaction for like is not to the creator address
	assert

	// contracts/WecoopFilePost.algo.ts:143
	// assert(
	//       platformCommunityFeeTxn.assetReceiver == this.manager_address.value,
	//       'Community coin payment is not being made to the manager address on liking file post'
	//     )
	frame_dig -5 // platformCommunityFeeTxn: AssetTransferTxn
	gtxns AssetReceiver
	bytec 2 //  "manager_address"
	app_global_get
	==

	// Community coin payment is not being made to the manager address on liking file post
	assert

	// contracts/WecoopFilePost.algo.ts:148
	// this.totalLikes.value += 1
	bytec 4 //  "totalLikes"
	app_global_get
	intc 0 // 1
	+
	bytec 4 //  "totalLikes"
	swap
	app_global_put

	// contracts/WecoopFilePost.algo.ts:150
	// this.filePosts(filePostId).value.likes += 1
	bytec 0 //  "filePost_"
	frame_dig -1 // filePostId: FilePostId
	concat
	box_get

	// box value does not exist: this.filePosts(filePostId).value
	assert
	store 255 // full array
	load 255 // full array
	pushint 40
	bytec 0 //  "filePost_"
	frame_dig -1 // filePostId: FilePostId
	concat
	box_get

	// box value does not exist: this.filePosts(filePostId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 40 8
	btoi
	intc 0 // 1
	+
	itob
	replace3
	bytec 0 //  "filePost_"
	frame_dig -1 // filePostId: FilePostId
	concat
	dup
	box_del
	pop
	swap
	box_put

	// contracts/WecoopFilePost.algo.ts:152
	// this.filePostLikes({ postId: filePostId }).value = {
	//       userAddress: this.txn.sender,
	//       timestamp: globals.latestTimestamp,
	//     }
	bytec 6 //  "filePostLike_"
	frame_dig -1 // filePostId: FilePostId
	concat
	txn Sender
	global LatestTimestamp
	itob
	concat
	box_put
	retsub

// replyFilePost((uint64),string,uint64,string,pay,pay,axfer,axfer)void
*abi_route_replyFilePost:
	// platformCommunityFeeTxn: axfer
	txn GroupIndex
	intc 0 // 1
	-
	dup
	gtxns TypeEnum
	intc 1 //  axfer
	==

	// argument 0 (platformCommunityFeeTxn) for replyFilePost must be a axfer transaction
	assert

	// creatorPayTxn: axfer
	txn GroupIndex
	intc 3 // 2
	-
	dup
	gtxns TypeEnum
	intc 1 //  axfer
	==

	// argument 1 (creatorPayTxn) for replyFilePost must be a axfer transaction
	assert

	// platformAlgoFeeTxn: pay
	txn GroupIndex
	intc 5 // 3
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 2 (platformAlgoFeeTxn) for replyFilePost must be a pay transaction
	assert

	// mbrTxn: pay
	txn GroupIndex
	intc 1 // 4
	-
	dup
	gtxns TypeEnum
	intc 0 //  pay
	==

	// argument 3 (mbrTxn) for replyFilePost must be a pay transaction
	assert

	// text: string
	txna ApplicationArgs 4
	extract 2 0

	// assetId: uint64
	txna ApplicationArgs 3
	btoi

	// country: string
	txna ApplicationArgs 2
	extract 2 0

	// filePostId: (uint64)
	txna ApplicationArgs 1
	dup
	len
	intc 4 // 8
	==

	// argument 7 (filePostId) for replyFilePost must be a (uint64)
	assert

	// execute replyFilePost((uint64),string,uint64,string,pay,pay,axfer,axfer)void
	callsub replyFilePost
	intc 0 // 1
	return

// replyFilePost(filePostId: FilePostId, country: string, assetId: AssetID, text: string, mbrTxn: PayTxn, platformAlgoFeeTxn: PayTxn, creatorPayTxn: AssetTransferTxn, platformCommunityFeeTxn: AssetTransferTxn): void
replyFilePost:
	proto 8 0

	// Push empty bytes after the frame pointer to reserve space for local variables
	bytec 1 // 0x

	// contracts/WecoopFilePost.algo.ts:168
	// repliedFilePost: FilePostData = this.filePosts(filePostId).value
	bytec 0 //  "filePost_"
	frame_dig -1 // filePostId: FilePostId
	concat
	frame_bury 0 // storage key//repliedFilePost

	// contracts/WecoopFilePost.algo.ts:171
	// assert(
	//       creatorPayTxn.assetReceiver == repliedFilePost.creator_address,
	//       'Creator fee transaction for reply is not to the creator address'
	//     )
	frame_dig -7 // creatorPayTxn: AssetTransferTxn
	gtxns AssetReceiver
	frame_dig 0 // storage key//repliedFilePost
	box_get

	// box value does not exist: this.filePosts(filePostId).value
	assert
	store 255 // full array
	load 255 // full array
	extract 0 32
	==

	// Creator fee transaction for reply is not to the creator address
	assert

	// contracts/WecoopFilePost.algo.ts:176
	// verifyPayTxn(platformAlgoFeeTxn, { receiver: this.manager_address.value })
	// verify receiver
	frame_dig -6 // platformAlgoFeeTxn: PayTxn
	gtxns Receiver
	bytec 2 //  "manager_address"
	app_global_get
	==

	// transaction verification failed: {"txn":"platformAlgoFeeTxn","field":"receiver","expected":"this.manager_address.value"}
	assert

	// contracts/WecoopFilePost.algo.ts:177
	// verifyPayTxn(mbrTxn, { receiver: this.app.address })
	// verify receiver
	frame_dig -5 // mbrTxn: PayTxn
	gtxns Receiver
	global CurrentApplicationAddress
	==

	// transaction verification failed: {"txn":"mbrTxn","field":"receiver","expected":"this.app.address"}
	assert

	// contracts/WecoopFilePost.algo.ts:180
	// assert(
	//       platformCommunityFeeTxn.assetReceiver == this.manager_address.value,
	//       'Community coin payment is not being made to the manager address on replying file post'
	//     )
	frame_dig -8 // platformCommunityFeeTxn: AssetTransferTxn
	gtxns AssetReceiver
	bytec 2 //  "manager_address"
	app_global_get
	==

	// Community coin payment is not being made to the manager address on replying file post
	assert

	// contracts/WecoopFilePost.algo.ts:185
	// assert(this.filePosts(filePostId).exists, 'Trying to reply to a non existing file post')
	bytec 0 //  "filePost_"
	frame_dig -1 // filePostId: FilePostId
	concat
	box_len
	swap
	pop

	// Trying to reply to a non existing file post
	assert

	// contracts/WecoopFilePost.algo.ts:187
	// this.filePostReplies({ postId: filePostId }).value = {
	//       userAddress: this.txn.sender,
	//       timestamp: globals.latestTimestamp,
	//       country: country,
	//       asset_id: assetId,
	//       text: text,
	//     }
	pushbytes 0x66696c65506f73745265706c795f // "filePostReply_"
	frame_dig -1 // filePostId: FilePostId
	concat
	dup
	box_del
	pop
	bytec 1 //  initial head
	bytec 1 //  initial tail
	pushbytes 0x0034 // initial head offset
	txn Sender
	callsub *process_static_tuple_element
	global LatestTimestamp
	itob
	callsub *process_static_tuple_element
	frame_dig -2 // country: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	frame_dig -3 // assetId: AssetID
	itob
	callsub *process_static_tuple_element
	frame_dig -4 // text: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	pop // pop head offset
	concat // concat head and tail
	box_put
	retsub

// getFilePostByPostId((uint64))(address,uint64,uint64,uint64,uint64,uint64,string,string,string,string)
*abi_route_getFilePostByPostId:
	// The ABI return prefix
	pushbytes 0x151f7c75

	// postId: (uint64)
	txna ApplicationArgs 1
	dup
	len
	intc 4 // 8
	==

	// argument 0 (postId) for getFilePostByPostId must be a (uint64)
	assert

	// execute getFilePostByPostId((uint64))(address,uint64,uint64,uint64,uint64,uint64,string,string,string,string)
	callsub getFilePostByPostId
	concat
	log
	intc 0 // 1
	return

// getFilePostByPostId(postId: FilePostId): FilePostData
getFilePostByPostId:
	proto 1 1

	// contracts/WecoopFilePost.algo.ts:198
	// assert(this.filePosts(postId).exists, 'File post does not exist')
	bytec 0 //  "filePost_"
	frame_dig -1 // postId: FilePostId
	concat
	box_len
	swap
	pop

	// File post does not exist
	assert

	// contracts/WecoopFilePost.algo.ts:199
	// return this.filePosts(postId).value;
	bytec 0 //  "filePost_"
	frame_dig -1 // postId: FilePostId
	concat
	box_get

	// box value does not exist: this.filePosts(postId).value
	assert
	retsub

*create_NoOp:
	pushbytes 0xb8447b36 // method "createApplication()void"
	txna ApplicationArgs 0
	match *abi_route_createApplication

	// this contract does not implement the given ABI method for create NoOp
	err

*call_NoOp:
	pushbytes 0x130ad35d // method "optinToAsset(uint64,pay)void"
	pushbytes 0x9556f63c // method "createFilePost(pay,axfer,string,string,string,string)void"
	pushbytes 0xc068db37 // method "likeFilePost((uint64),pay,pay,axfer,axfer)void"
	pushbytes 0x84373eeb // method "replyFilePost((uint64),string,uint64,string,pay,pay,axfer,axfer)void"
	pushbytes 0x436b8775 // method "getFilePostByPostId((uint64))(address,uint64,uint64,uint64,uint64,uint64,string,string,string,string)"
	txna ApplicationArgs 0
	match *abi_route_optinToAsset *abi_route_createFilePost *abi_route_likeFilePost *abi_route_replyFilePost *abi_route_getFilePostByPostId

	// this contract does not implement the given ABI method for call NoOp
	err

*process_static_tuple_element:
	proto 4 3
	frame_dig -4 // tuple head
	frame_dig -1 // element
	concat
	frame_dig -3 // tuple tail
	frame_dig -2 // head offset
	retsub

*process_dynamic_tuple_element:
	proto 4 3
	frame_dig -4 // tuple head
	frame_dig -2 // head offset
	concat
	frame_bury -4 // tuple head
	frame_dig -1 // element
	dup
	len
	frame_dig -2 // head offset
	btoi
	+
	itob
	extract 6 2
	frame_bury -2 // head offset
	frame_dig -3 // tuple tail
	swap
	concat
	frame_bury -3 // tuple tail
	frame_dig -4 // tuple head
	frame_dig -3 // tuple tail
	frame_dig -2 // head offset
	retsub